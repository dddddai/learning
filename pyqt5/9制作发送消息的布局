import sys
from PyQt5.QtWidgets import (QWidget, QLabel, QLineEdit,
    QTextEdit, QGridLayout, QApplication)



class Example(QWidget):
    def __init__(self):
        super().__init__()
        self.initUI()

    def initUI(self):
        title=QLabel('Ttile')
        autor=QLabel('Author')
        msg=QLabel('Message')
        #创建了一个有三个标签的窗口。两个行编辑和一个文版编辑，这是用QGridLayout模块搞定的。

        grid=QGridLayout()
        self.setLayout(grid)
        grid.setSpacing(10)

        grid.addWidget(title,1,0)
        grid.addWidget(QLineEdit(),1,1)
        grid.addWidget(autor,2,0)
        grid.addWidget(QLineEdit(),2,1)
        grid.addWidget(msg,3,0)
        grid.addWidget(QTextEdit(),3,1,7,1)

        self.setGeometry(700,300,500,300)
        self.setWindowTitle('Message')
        self.show()



if __name__ == '__main__':
    app=QApplication(sys.argv)
    exp=Example()
    sys.exit(app.exec_())
    

    
 
其中grid.setSpacing(10) 是设定部件之间的距离是10个像素。

grid.addWidget(reviewEdit,3,1,5,1)
把这个部件放到了第3行，第1列
并且后面的5和1，分别代表所占的行数和列数，就是表示放到第三行，大小占5行。    
