Given an integer with no more than 9 digits, you are supposed to read it in the traditional Chinese way. 
Output “Fu” first if it is negative.
For example, -123456789 is read as “Fu yi Yi er Qian san Bai si Shi wu Wan liu Qian qi Bai ba Shi jiu”. 
Note: zero (“ling”) must be handled correctly according to the Chinese tradition.
For example, 100800 is “yi Shi Wan ling ba Bai”.

Input Specification:
Each input file contains one test case, which gives an integer with no more than 9 digits.

Output Specification:
For each test case, print in a line the Chinese way of reading the number. 
The characters are separated by a space and there must be no extra space at the end of the line.

Sample Input 1:
-123456789

Sample Output 1:
Fu yi Yi er Qian san Bai si Shi wu Wan liu Qian qi Bai ba Shi jiu


Sample Input 2:
100800

Sample Output 2:
yi Shi Wan ling ba Bai





#include<bits/stdc++.h>
using namespace std;

string a[10]={"ling","yi","er","san","si","wu","liu","qi","ba","jiu"};
string b[4]={"","Shi","Bai","Qian"};

string read(string num,bool flag){
    int end=num.length()-1;
    while(end>=0 && num[end]=='0'){
        --end;
    }
    if(end==-1){
        return "ling";
    }
    string result;
    for(int i=0;i<=end;){
        if(result.length()){
            result.push_back(' ');
        }
        if(num[i]=='0'){
            if(i || flag){
                result+="ling";
            }
            while(i<=end && num[i]=='0'){
                ++i;
            }
        }else{
            result+=a[num[i]-'0'];
            if(i<num.length()-1){
                result+=" "+b[num.length()-1-i];
            }
            ++i;
        }
    }
    return result;
}

int main(){
    string s;
    cin>>s;
    if(s[0]=='-'){
        cout<<"Fu ";
        s=s.substr(1);
    }
    if(s.length()==9){
        cout<<a[s[0]-'0']<<" Yi ";
        s=s.substr(1);
    }
    if(s.length()<=4){
        cout<<read(s,false);
    }else{
        cout<<read(s.substr(0,s.length()-4),s.length()==9)<<" Wan "<<read(s.substr(s.length()-4),true);
    }
    return 0;
}
